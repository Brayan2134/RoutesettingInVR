import SwiftUI

@main
struct HelloARWorldApp: App {
    // The main entry point of the app.
    var body: some Scene {
        WindowGroup {
            // ContentView is the root view of the app.
            ContentView()
        }
    }
}
